# Frontend Environment Variables for Diambars-Sublim-Public
# Only include variables needed by the frontend (must start with VITE_)

# QUICK SWITCH: Change this to switch between backends
# Options: "local" or "render"
VITE_BACKEND_MODE=local

# API Configuration (will be overridden by VITE_BACKEND_MODE)
VITE_API_URL=http://localhost:4000/api

# Environment
VITE_NODE_ENV=production

# Backend URLs for quick reference
# Local:  http://localhost:4000/api
# Render: https://expo2025-8bjn.onrender.com/api